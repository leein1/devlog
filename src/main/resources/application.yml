spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devlog?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: dkfrhflwmawhgdk
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: DEBUG #??? ?? service??? ??? ??, contorller?? ???? ?? ?? ???? ??
