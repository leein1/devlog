spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devlog?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: dkfrhflwmawhgdk
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: DEBUG #영속성 범위 service레이어 까지로 제한, contorller에서 의도하지 않은 쿼리 지연로딩 방지
